# 视频配音 Agent

独立的视频配音 Agent：在对话中接收目标语言和风格，使用 Whisper 转写，再把翻译后的系统语音与原视频混音，右侧返回预览和逐段对照。

Agent 声明百炼 provider 时会自动继承桌面已配置的百炼凭据，用于翻译或改写文案，不会显示文本模型选择器。需要本机 `whisper`、`ffmpeg` 和 macOS `say`；没有百炼凭据时仍支持同语言直接配音。
